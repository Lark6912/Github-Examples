name: "Testing commands"

on: ['push']

jobs: 
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: "group logging"
        run: | 
          echo "::group:: My Group Messages"
          echo "Msg 1"
          echo "Msg 2"
          echo "::endgroup::"
      - name: "Step one"
        run: |
          echo "MY_VAL=hello" >> $GITHUB_ENV
      - name: "Step two"
        run: |
          echo $MY_VAL 