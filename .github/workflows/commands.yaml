name: "Testing commands"

on: ['push']

jobs: 
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: "group logging"
        run: | 
          echo "::group:: My Group Messages"
          echo "Msg 1"
          echo "Msg 2"
          echo "::endgroup::"
